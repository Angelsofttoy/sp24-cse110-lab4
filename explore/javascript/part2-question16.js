let statistics = {
    redCar: 21,
    blueCars: 45,
    greenCars: 12,
    raceCars: 5,
    blackCars: 40, 
    rareCars: 2
};

function rOrEven(statistics) {
    let list = Object.keys(statistics);
    let valueList = Object.values(statistics);
    for(i = 0; i < discounted.length; i++){
        let key = key[i];
        let value = value[i];
        if(key.startWith('r') || value % 2 !== 0) {
            console.log(value);
        }
    }
}

